import React from "react";
import ReactDOM from "react-dom/client";

const title = <h1 className="title"> Hello welcome to React Js world </h1>;
const author = <h4 className="author"> Developer Name : Saravanan </h4>;
const fruitArray = ["apple", "orange", "grapes", "banana"];
const isFruitAvailable = true;
const vegtableListstatus = true;


const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
  <React.StrictMode>
   
  </React.StrictMode>,
);
